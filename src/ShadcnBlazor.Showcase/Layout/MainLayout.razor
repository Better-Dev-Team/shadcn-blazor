@namespace ShadcnBlazor.Showcase.Layout
@inherits LayoutComponentBase
@implements IDisposable
@inject IThemeService ThemeService

<ThemeProvider>
    <div class="relative flex min-h-screen flex-col bg-background text-foreground">
        <header class="sticky top-0 z-40 w-full border-b border-border/40 bg-background/95">
            <div class="container mx-auto flex h-14 max-w-3xl items-center justify-between px-4">
                <a href="" class="flex items-center space-x-2 font-bold tracking-tight">
                    <div class="flex h-6 w-6 items-center justify-center rounded-md bg-primary text-primary-foreground">
                        <svg class="h-4 w-4" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5">
                            <polygon points="12 2 2 7 12 12 22 7 12 2"></polygon>
                            <polyline points="2 17 12 22 22 17"></polyline>
                            <polyline points="2 12 12 17 22 12"></polyline>
                        </svg>
                    </div>
                    <span class="text-base font-extrabold">shadcn/blazor</span>
                    <Badge Variant="BadgeVariant.Secondary">showcase</Badge>
                </a>

                <div class="flex items-center gap-2">
                    <Button Variant="ButtonVariant.Ghost" Size="ButtonSize.Icon" Class="h-9 w-9" OnClick="ToggleThemeModeAsync">
                        @if (ThemeService.IsDark)
                        {
                            <svg class="h-4 w-4" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                                <circle cx="12" cy="12" r="4"/><path d="M12 2v2"/><path d="M12 20v2"/><path d="m4.93 4.93 1.41 1.41"/><path d="m17.66 17.66 1.41 1.41"/><path d="M2 12h2"/><path d="M20 12h2"/><path d="m6.34 17.66-1.41 1.41"/><path d="m19.07 4.93-1.41 1.41"/>
                            </svg>
                        }
                        else
                        {
                            <svg class="h-4 w-4" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                                <path d="M12 3a6 6 0 0 0 9 9 9 9 0 1 1-9-9Z"/>
                            </svg>
                        }
                        <span class="sr-only">Toggle theme</span>
                    </Button>
                </div>
            </div>
        </header>

        <main class="flex-1">
            @Body
        </main>

        <footer class="border-t border-border/40 py-6">
            <div class="container mx-auto max-w-3xl px-4 text-center text-sm text-muted-foreground">
                Built with Blazor + ShadcnBlazor. Run it locally: <span class="font-mono text-xs">dotnet run --project src/ShadcnBlazor.Showcase</span>
            </div>
        </footer>

        <Toaster Position="ToastPosition.BottomRight" />
    </div>
</ThemeProvider>

@code {
    protected override void OnInitialized()
    {
        ThemeService.OnChange += OnThemeChanged;
    }

    public void Dispose()
    {
        ThemeService.OnChange -= OnThemeChanged;
    }

    private void OnThemeChanged() => InvokeAsync(StateHasChanged);

    private async Task ToggleThemeModeAsync()
    {
        var newMode = ThemeService.IsDark ? ThemeMode.Light : ThemeMode.Dark;
        await ThemeService.SetModeAsync(newMode);
    }
}
